// worker-bootstrap.js is a Web-Worker entry script (loaded by path into a
// Worker by the VOIP stack), not an ESM module — it intentionally exports
// nothing. This file exists so every .js in lib/ has a matching .d.ts.
export {};
