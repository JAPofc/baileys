// worker-modules.js is a vendored Meta/FB WASM worker bundle (refreshed by
// scripts/fetch-wasm-resources.mjs) consumed by path, not an ESM module — it
// intentionally exports nothing. This file exists so every .js in the
// package has a matching .d.ts.
export {};
