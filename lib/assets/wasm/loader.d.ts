// loader.js is a vendored, minified Meta/FB module-registry bundle consumed by
// path (see lib/VoIP/wasm-engine.js), not an ESM module — it intentionally
// exports nothing. This file exists so every .js in the package has a
// matching .d.ts.
export {};
